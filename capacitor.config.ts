import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vlaamse_vinyl',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
